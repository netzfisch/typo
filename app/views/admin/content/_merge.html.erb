<h4><%= _("Merge Articles") %></h4>
<%= form_tag(:controller => "admin/content", :action => "merge", :id => params[:id], :method => "put") do %>
  <div>
    <%= label_tag(:merge_with, "Article ID") %>
    <%= text_field_tag(:merge_with) %>
     <div class="form-actions">
       <%= submit_tag("Merge") %>
     </div>
  </div>
<% end %>

